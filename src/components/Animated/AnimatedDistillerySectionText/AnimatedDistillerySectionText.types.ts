import { Strings } from '@/types/types';

export interface IProps {
  title: string;
  region: string;
  owner: string;
  location: string;
  subtitle: string;
  equipment: Strings;
  history: string;
  inView: boolean;
}
