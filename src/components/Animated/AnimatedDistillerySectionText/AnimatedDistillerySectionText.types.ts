import { Strings } from '@/types/types';

export interface IProps {
  region: string;
  owner: string;
  location: string;
  subtitle: string;
  equipment: Strings;
  history: string;
  inView: boolean;
}
